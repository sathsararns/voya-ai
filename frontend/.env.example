# Backend base URL the frontend talks to. Defaults to http://127.0.0.1:8000
# (see src/lib/api.ts) if unset — override here only if that port is taken
# by something else on your machine, or the backend is running elsewhere.
VITE_API_BASE_URL=http://127.0.0.1:8000
